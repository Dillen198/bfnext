{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core events/window APIs. The app's own commands are Rust functions registered in lib.rs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
